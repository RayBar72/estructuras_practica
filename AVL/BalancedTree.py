from logging import root
from AVL.Node import Node

class BalancedTree(object):
	def __init__(self):
		self.root = root

	def insert(sefl):
		parent = self.root
		if self.root == None:
			parent = Node()